name: test_build
on: workflow_dispatch
jobs:
  test_build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          python3 -m venv venv
          source venv/bin/activate
          pip3 install esphome
          ls -a
          esphome compile sample-esp32.yaml
